{
  "name": "translategemma-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:sidecar": "node ./scripts/build-sidecar.mjs",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:debug": "tauri build --debug",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.1"
  }
}
